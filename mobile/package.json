{
  "name": "tgexplorer-mobile",
  "private": true,
  "version": "1.6.0",
  "description": "Telegram Explorer — native shell (Capacitor) for Android/iOS",
  "scripts": {
    "sync": "node sync-web.js && npx cap sync",
    "add:android": "node sync-web.js && npx cap add android",
    "add:ios": "node sync-web.js && npx cap add ios",
    "open:android": "npx cap open android",
    "open:ios": "npx cap open ios",
    "run:android": "node sync-web.js && npx cap sync android && npx cap run android"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0",
    "@capacitor/ios": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
